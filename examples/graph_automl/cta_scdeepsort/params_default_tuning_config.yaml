---
type: preprocessor
tune_mode: params
pipeline:
  - type: graph.cell
    target: PCACellFeatureGraph
    params:
      n_components: 200
      split_name: train
    params_to_tune:
      normalize_edges:
        values: [True]
  - type: misc
    target: SetConfig
    params:
      config_dict:
        label_channel: cell_type
wandb:
  entity: xzy11632
  project: dance-dev
  method: grid
  metric:
    name: combined_score
    goal: maximize
